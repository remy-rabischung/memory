import React from 'react';
import ReactDOM from 'react-dom';
import App from '/src/App.jsx'; // Assurez-vous que le chemin vers votre composant App.jsx est correct

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
